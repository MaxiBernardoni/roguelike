extends Object
class_name Style

## Global palette — no textures; use with draw(), ColorRect, modulate.

const PLAYER := Color(0.28, 0.62, 0.95, 1.0)
const ENEMY := Color(0.92, 0.32, 0.28, 1.0)

## Chess-board floor (alternating).
const GRID_LIGHT := Color(0.11, 0.13, 0.17, 1.0)
const GRID_DARK := Color(0.15, 0.17, 0.21, 1.0)

## Subtle cell edge (not bright white).
const GRID_BORDER := Color(0.22, 0.24, 0.28, 0.55)

## Preview / targeting overlays (semi-transparent).
const HIGHLIGHT_PATH := Color(0.35, 0.78, 0.48, 0.42)
const HIGHLIGHT_TARGET := Color(0.95, 0.82, 0.28, 0.55)

## UI chrome.
const UI_PANEL := Color(0.06, 0.07, 0.09, 0.94)
const UI_BORDER := Color(0.28, 0.31, 0.38, 1.0)
const UI_TEXT := Color(0.92, 0.93, 0.95, 1.0)
const UI_MUTED := Color(0.55, 0.58, 0.65, 1.0)

## Energy pips.
const ENERGY_FULL := Color(0.42, 0.82, 0.52, 1.0)
const ENERGY_EMPTY := Color(0.2, 0.21, 0.23, 1.0)
const ENERGY_BORDER := Color(0.35, 0.38, 0.44, 1.0)
